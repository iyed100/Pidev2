<<<<<<< HEAD
{% extends 'base.html.twig' %}

{% block stylesheets %}
    {{ parent() }}
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css">
    <style>
        :root {
            --primary-color: #6a11cb;
            --secondary-color: #2575fc;
        }
        
        /* Hero Section */
        .hero-section {
            background: url('/images/background.jpg') center/cover no-repeat;
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            text-align: center;
            color: white;
            position: relative;
        }
        
        .navbar {
            background: rgba(0, 0, 0, 0.5) !important;
        }
        
        .navbar a { 
            color: white !important; 
            font-weight: 500;
        }
        
        .hero-text h1 {
            font-size: 3.5rem;
            font-weight: 700;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
            margin-bottom: 1rem;
        }
        
        .hero-text p {
            font-size: 1.5rem;
            text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.5);
        }
        
        .booking-form {
    background: rgba(255, 255, 255, 0.95);
    padding: 25px;
    border-radius: 12px;
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.15);
    width: 90%;
    max-width: 1200px;
}

.form-select-lg, .form-control-lg {
    padding: 12px 15px;
    font-size: 1rem;
    border: 1px solid #e0e0e0;
}

.form-label {
    margin-bottom: 8px;
    display: block;
    color: #555;
}

.btn-lg {
    padding: 12px;
    font-size: 1.1rem;
    font-weight: 600;
}
        
        /* Offers Section */
        .offers-section {
            padding: 150px 0 80px;
            background-color: #f8f9fa;
        }
        
        .page-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 40px;
        }
        
        .page-title {
            color: #2c3e50;
            font-weight: 700;
            font-size: 2.2rem;
            position: relative;
            padding-bottom: 10px;
        }
        
        .page-title::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 0;
            width: 80px;
            height: 4px;
            background: linear-gradient(to right, var(--primary-color), var(--secondary-color));
        }
        
        .nav-arrows {
            display: flex;
            gap: 15px;
        }
        
        .arrow-btn {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            background: white;
            color: var(--primary-color);
            border: 1px solid #ddd;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: all 0.3s;
        }
        
        .arrow-btn:hover {
            background: linear-gradient(to right, var(--primary-color), var(--secondary-color));
            color: white;
            transform: translateY(-2px);
        }
        
        /* Cards */
        .card-special {
            border: none;
            border-radius: 15px;
            overflow: hidden;
            transition: all 0.3s ease;
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
            margin-bottom: 30px;
            background: white;
            height: 100%;
        }
        
        .card-special:hover {
            transform: translateY(-10px);
            box-shadow: 0 15px 30px rgba(0,0,0,0.2);
        }
        
        .card-img-container {
            height: 200px;
            overflow: hidden;
            position: relative;
        }
        
        .card-img-top {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: transform 0.5s;
        }
        
        .card-special:hover .card-img-top {
            transform: scale(1.05);
        }
        
        .card-tag {
            position: absolute;
            top: 20px;
            right: 20px;
            background: rgba(255, 255, 255, 0.9);
            padding: 5px 15px;
            border-radius: 20px;
            font-size: 0.8rem;
            font-weight: 600;
            color: var(--primary-color);
            box-shadow: 0 2px 5px rgba(0,0,0,0.1);
        }
        
        .card-body {
            padding: 1.5rem;
        }
        
        .card-title {
            font-weight: 700;
            color: #2c3e50;
            margin-bottom: 0.5rem;
        }
        
        .star-rating {
            color: #FFD700;
            font-size: 1rem;
            margin-bottom: 1rem;
        }
        
        .card-text {
            color: #7f8c8d;
            margin-bottom: 1.5rem;
        }
        
        .price-container {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        
        .price-tag {
            font-size: 1.5rem;
            font-weight: 700;
            color: var(--primary-color);
        }
        
        .btn-details {
            background: linear-gradient(to right, var(--primary-color), var(--secondary-color));
            border: none;
            border-radius: 50px;
            padding: 8px 20px;
            font-weight: 600;
            color: white;
            transition: all 0.3s;
            font-size: 0.9rem;
        }
        
        .btn-details:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(106, 17, 203, 0.4);
        }
        /* Events Section */
.events-section {
    position: relative;
    background-color: #f8f9fa;
}

.section-title {
    font-size: 2.5rem;
    font-weight: 700;
    color: #2c3e50;
    position: relative;
    display: inline-block;
    margin-bottom: 1.5rem;
}

.section-title::after {
    content: '';
    position: absolute;
    bottom: -10px;
    left: 50%;
    transform: translateX(-50%);
    width: 80px;
    height: 4px;
    background: linear-gradient(to right, #6a11cb, #2575fc);
}

.citation-text {
    max-width: 700px;
    margin: 0 auto;
    line-height: 1.6;
}

.event-card {
    border-radius: 12px;
    transition: transform 0.3s ease, box-shadow 0.3s ease;
}

.event-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 10px 25px rgba(0,0,0,0.1);
}

.event-image-container {
    position: relative;
    overflow: hidden;
}

.event-tag {
    position: absolute;
    top: 15px;
    right: 15px;
    background: rgba(255,255,255,0.9);
    padding: 5px 15px;
    border-radius: 20px;
    font-size: 0.8rem;
    font-weight: 600;
    color: #6a11cb;
    box-shadow: 0 2px 5px rgba(0,0,0,0.1);
}

.event-title {
    font-size: 1.5rem;
    font-weight: 700;
    color: #2c3e50;
    margin-bottom: 0.5rem;
}

.event-description {
    color: #7f8c8d;
    line-height: 1.6;
}

.event-duration {
    font-weight: 600;
    color: #6a11cb;
}

.event-price {
    font-size: 1.2rem;
    font-weight: 700;
    color: #2575fc;
}

.inspiration-box {
    border-left: 4px solid #6a11cb;
    border-radius: 4px;
    max-width: 300px;
}
/* Destinations Section */
.destinations-section {
    position: relative;
}

.section-header {
    padding-bottom: 1rem;
    border-bottom: 1px solid #eee;
}

.destinations-carousel {
    overflow: hidden;
    position: relative;
}

.destinations-track {
    display: flex;
    gap: 20px;
    transition: transform 0.5s ease;
    padding-bottom: 20px;
}

.destination-card {
    min-width: 280px;
    border-radius: 12px;
    overflow: hidden;
    box-shadow: 0 5px 15px rgba(0,0,0,0.1);
    transition: transform 0.3s ease;
    background: white;
}

.destination-card:hover {
    transform: translateY(-5px);
}

.destination-card .card-img-top {
    height: 180px;
    position: relative;
    overflow: hidden;
}

.destination-card img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.5s ease;
}

.destination-card:hover img {
    transform: scale(1.05);
}

.location-badge {
    position: absolute;
    bottom: 15px;
    left: 15px;
    background: rgba(0,0,0,0.7);
    color: white;
    padding: 5px 10px;
    border-radius: 20px;
    font-size: 0.8rem;
    display: flex;
    align-items: center;
}

.location-badge i {
    margin-right: 5px;
    color: #ff4757;
}

.rating-badge {
    position: absolute;
    top: 15px;
    right: 15px;
    background: white;
    padding: 5px 10px;
    border-radius: 20px;
    font-weight: 600;
    box-shadow: 0 2px 5px rgba(0,0,0,0.1);
}

.rating-badge i {
    color: #FFD700;
    margin-right: 3px;
}

.destinations-arrows {
    display: flex;
    gap: 10px;
}

.arrow-btn {
    width: 40px;
    height: 40px;
    border-radius: 50%;
    background: white;
    color: #6a11cb;
    border: 1px solid #ddd;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: all 0.3s;
    cursor: pointer;
}

.arrow-btn:hover {
    background: linear-gradient(to right, #6a11cb, #2575fc);
    color: white;
    transform: translateY(-2px);
}
    /* Destinations Carousel */
        .destinations-carousel {
            overflow: hidden;
            position: relative;
            width: 100%;
        }
        
        .destinations-track {
            display: flex;
            gap: 20px;
            transition: transform 0.5s ease-in-out;
            padding-bottom: 20px;
            will-change: transform;
        }
        
        .destination-card {
            flex: 0 0 auto;
            width: 280px;
            border-radius: 12px;
            overflow: hidden;
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
            transition: all 0.3s ease;
            background: white;
        }
        
        .location-badge {
            position: absolute;
            bottom: 15px;
            left: 15px;
            background: rgba(0,0,0,0.7);
            color: white;
            padding: 5px 10px;
            border-radius: 20px;
            font-size: 0.8rem;
            display: flex;
            align-items: center;
        }
        
        .destinations-arrows .arrow-btn[style*="hidden"] {
            opacity: 0.3;
            cursor: not-allowed;
        }
    </style>
{% endblock %}

{% block body %}
    <!-- Hero Section -->
    <header class="hero-section">
        <nav class="navbar navbar-expand-lg navbar-dark fixed-top">
    <div class="container">
        <a class="navbar-brand" href="#">
            <img src="{{ asset('images/logo.png') }}" alt="Logo" width="50" height="50">
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto">
                <li class="nav-item"><a class="nav-link" href="#">Home</a></li>
                <li class="nav-item"><a class="nav-link" href="#">Hébergement</a></li>
                <li class="nav-item"><a class="nav-link" href="#">Espace de coworking</a></li>
                <li class="nav-item"><a class="nav-link" href="#">Événements</a></li>
                <li class="nav-item"><a class="nav-link" href="#">Avis et réclamation</a></li>
            </ul>

            {% if user %}
                <span class="navbar-text text-white me-3">
                     <a href="{{ path('app_client_dashboard') }}" class="user-profile-link">
                        Bonjour, {{ user.nom }} {{ user.prenom }}
                    </a>
                </span>
                <a href="{{ path('app_logout') }}" class="btn btn-outline-light">Logout</a>
            {% else %}
                <a href="{{ path('app_login') }}" class="btn btn-outline-light me-2">Login</a>
                <a href="{{ path('app_register') }}" class="btn btn-danger">Sign up</a>
            {% endif %}
        </div>
    </div>
</nav>

        <div class="hero-text container">
            <h1 class="animate__animated animate__fadeInDown">Commencez votre inoubliable voyage d'affaires avec nous.</h1>
            <p class="animate__animated animate__fadeInUp animate__delay-1s">Le meilleur voyage pour votre voyage commence dès maintenant.</p>
        </div>
        
      <div class="booking-form animate__animated animate__fadeInUp animate__delay-2s">
    <form class="row g-3 align-items-end">
        <div class="col-md-4">
            <div class="form-group">
                <label class="form-label fw-bold">Destination</label>
                <select class="form-select form-select-lg">
                    <option selected disabled>Choisissez une destination</option>
                    <option>Dubai</option>
                    <option>Paris</option>
                    <option>New York</option>
                    <option>Lisbonne</option>
                    <option>Rome</option>
                </select>
            </div>
        </div>
        
        <div class="col-md-2">
            <div class="form-group">
                <label class="form-label fw-bold">Personnes</label>
                <input type="number" class="form-control form-control-lg" value="2" min="1">
            </div>
        </div>
        
        <div class="col-md-2">
            <div class="form-group">
                <label class="form-label fw-bold">Arrivée</label>
                <input type="date" class="form-control form-control-lg">
            </div>
        </div>
        
        <div class="col-md-2">
            <div class="form-group">
                <label class="form-label fw-bold">Départ</label>
                <input type="date" class="form-control form-control-lg">
            </div>
        </div>
        
        <div class="col-md-2">
            <button class="btn btn-danger btn-lg w-100 py-2">
                <i class="fas fa-search me-2"></i> Rechercher
            </button>
        </div>
    </form>
</div>
    </header>
=======
    {% extends 'base.html.twig' %}

    {% block stylesheets %}
        {{ parent() }}
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css">
        <style>
            :root {
                --primary-color: #6a11cb;
                --secondary-color: #2575fc;
            }
            
            /* Hero Section */
            .hero-section {
                background: url('/images/background.jpg') center/cover no-repeat;
                height: 100vh;
                display: flex;
                align-items: center;
                justify-content: center;
                text-align: center;
                color: white;
                position: relative;
            }
            
            .navbar {
                background: rgba(0, 0, 0, 0.5) !important;
            }
            
            .navbar a { 
                color: white !important; 
                font-weight: 500;
            }
            
            .hero-text h1 {
                font-size: 3.5rem;
                font-weight: 700;
                text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
                margin-bottom: 1rem;
            }
            
            .hero-text p {
                font-size: 1.5rem;
                text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.5);
            }
            
            .booking-form {
        background: rgba(255, 255, 255, 0.95);
        padding: 25px;
        border-radius: 12px;
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.15);
        width: 90%;
        max-width: 1200px;
    }

    .form-select-lg, .form-control-lg {
        padding: 12px 15px;
        font-size: 1rem;
        border: 1px solid #e0e0e0;
    }

    .form-label {
        margin-bottom: 8px;
        display: block;
        color: #555;
    }

    .btn-lg {
        padding: 12px;
        font-size: 1.1rem;
        font-weight: 600;
    }
            
            /* Offers Section */
            .offers-section {
                padding: 150px 0 80px;
                background-color: #f8f9fa;
            }
            
            .page-header {
                display: flex;
                justify-content: space-between;
                align-items: center;
                margin-bottom: 40px;
            }
            
            .page-title {
                color: #2c3e50;
                font-weight: 700;
                font-size: 2.2rem;
                position: relative;
                padding-bottom: 10px;
            }
            
            .page-title::after {
                content: '';
                position: absolute;
                bottom: 0;
                left: 0;
                width: 80px;
                height: 4px;
                background: linear-gradient(to right, var(--primary-color), var(--secondary-color));
            }
            
            .nav-arrows {
                display: flex;
                gap: 15px;
            }
            
            .arrow-btn {
                width: 40px;
                height: 40px;
                border-radius: 50%;
                background: white;
                color: var(--primary-color);
                border: 1px solid #ddd;
                display: flex;
                align-items: center;
                justify-content: center;
                transition: all 0.3s;
            }
            
            .arrow-btn:hover {
                background: linear-gradient(to right, var(--primary-color), var(--secondary-color));
                color: white;
                transform: translateY(-2px);
            }
            
            /* Cards */
            .card-special {
                border: none;
                border-radius: 15px;
                overflow: hidden;
                transition: all 0.3s ease;
                box-shadow: 0 5px 15px rgba(0,0,0,0.1);
                margin-bottom: 30px;
                background: white;
                height: 100%;
            }
            
            .card-special:hover {
                transform: translateY(-10px);
                box-shadow: 0 15px 30px rgba(0,0,0,0.2);
            }
            
            .card-img-container {
                height: 200px;
                overflow: hidden;
                position: relative;
            }
            
            .card-img-top {
                width: 100%;
                height: 100%;
                object-fit: cover;
                transition: transform 0.5s;
            }
            
            .card-special:hover .card-img-top {
                transform: scale(1.05);
            }
            
            .card-tag {
                position: absolute;
                top: 20px;
                right: 20px;
                background: rgba(255, 255, 255, 0.9);
                padding: 5px 15px;
                border-radius: 20px;
                font-size: 0.8rem;
                font-weight: 600;
                color: var(--primary-color);
                box-shadow: 0 2px 5px rgba(0,0,0,0.1);
            }
            
            .card-body {
                padding: 1.5rem;
            }
            
            .card-title {
                font-weight: 700;
                color: #2c3e50;
                margin-bottom: 0.5rem;
            }
            
            .star-rating {
                color: #FFD700;
                font-size: 1rem;
                margin-bottom: 1rem;
            }
            
            .card-text {
                color: #7f8c8d;
                margin-bottom: 1.5rem;
            }
            
            .price-container {
                display: flex;
                justify-content: space-between;
                align-items: center;
            }
            
            .price-tag {
                font-size: 1.5rem;
                font-weight: 700;
                color: var(--primary-color);
            }
            
            .btn-details {
                background: linear-gradient(to right, var(--primary-color), var(--secondary-color));
                border: none;
                border-radius: 50px;
                padding: 8px 20px;
                font-weight: 600;
                color: white;
                transition: all 0.3s;
                font-size: 0.9rem;
            }
            
            .btn-details:hover {
                transform: translateY(-2px);
                box-shadow: 0 5px 15px rgba(106, 17, 203, 0.4);
            }
            /* Events Section */
    .events-section {
        position: relative;
        background-color: #f8f9fa;
    }

    .section-title {
        font-size: 2.5rem;
        font-weight: 700;
        color: #2c3e50;
        position: relative;
        display: inline-block;
        margin-bottom: 1.5rem;
    }

    .section-title::after {
        content: '';
        position: absolute;
        bottom: -10px;
        left: 50%;
        transform: translateX(-50%);
        width: 80px;
        height: 4px;
        background: linear-gradient(to right, #6a11cb, #2575fc);
    }

    .citation-text {
        max-width: 700px;
        margin: 0 auto;
        line-height: 1.6;
    }

    .event-card {
        border-radius: 12px;
        transition: transform 0.3s ease, box-shadow 0.3s ease;
    }

    .event-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 10px 25px rgba(0,0,0,0.1);
    }

    .event-image-container {
        position: relative;
        overflow: hidden;
    }

    .event-tag {
        position: absolute;
        top: 15px;
        right: 15px;
        background: rgba(255,255,255,0.9);
        padding: 5px 15px;
        border-radius: 20px;
        font-size: 0.8rem;
        font-weight: 600;
        color: #6a11cb;
        box-shadow: 0 2px 5px rgba(0,0,0,0.1);
    }

    .event-title {
        font-size: 1.5rem;
        font-weight: 700;
        color: #2c3e50;
        margin-bottom: 0.5rem;
    }

    .event-description {
        color: #7f8c8d;
        line-height: 1.6;
    }

    .event-duration {
        font-weight: 600;
        color: #6a11cb;
    }

    .event-price {
        font-size: 1.2rem;
        font-weight: 700;
        color: #2575fc;
    }

    .inspiration-box {
        border-left: 4px solid #6a11cb;
        border-radius: 4px;
        max-width: 300px;
    }
    /* Destinations Section */
    .destinations-section {
        position: relative;
    }

    .section-header {
        padding-bottom: 1rem;
        border-bottom: 1px solid #eee;
    }

    .destinations-carousel {
        overflow: hidden;
        position: relative;
    }

    .destinations-track {
        display: flex;
        gap: 20px;
        transition: transform 0.5s ease;
        padding-bottom: 20px;
    }

    .destination-card {
        min-width: 280px;
        border-radius: 12px;
        overflow: hidden;
        box-shadow: 0 5px 15px rgba(0,0,0,0.1);
        transition: transform 0.3s ease;
        background: white;
    }

    .destination-card:hover {
        transform: translateY(-5px);
    }

    .destination-card .card-img-top {
        height: 180px;
        position: relative;
        overflow: hidden;
    }

    .destination-card img {
        width: 100%;
        height: 100%;
        object-fit: cover;
        transition: transform 0.5s ease;
    }

    .destination-card:hover img {
        transform: scale(1.05);
    }

    .location-badge {
        position: absolute;
        bottom: 15px;
        left: 15px;
        background: rgba(0,0,0,0.7);
        color: white;
        padding: 5px 10px;
        border-radius: 20px;
        font-size: 0.8rem;
        display: flex;
        align-items: center;
    }

    .location-badge i {
        margin-right: 5px;
        color: #ff4757;
    }

    .rating-badge {
        position: absolute;
        top: 15px;
        right: 15px;
        background: white;
        padding: 5px 10px;
        border-radius: 20px;
        font-weight: 600;
        box-shadow: 0 2px 5px rgba(0,0,0,0.1);
    }

    .rating-badge i {
        color: #FFD700;
        margin-right: 3px;
    }

    .destinations-arrows {
        display: flex;
        gap: 10px;
    }

    .arrow-btn {
        width: 40px;
        height: 40px;
        border-radius: 50%;
        background: white;
        color: #6a11cb;
        border: 1px solid #ddd;
        display: flex;
        align-items: center;
        justify-content: center;
        transition: all 0.3s;
        cursor: pointer;
    }

    .arrow-btn:hover {
        background: linear-gradient(to right, #6a11cb, #2575fc);
        color: white;
        transform: translateY(-2px);
    }
        /* Destinations Carousel */
            .destinations-carousel {
                overflow: hidden;
                position: relative;
                width: 100%;
            }
            
            .destinations-track {
                display: flex;
                gap: 20px;
                transition: transform 0.5s ease-in-out;
                padding-bottom: 20px;
                will-change: transform;
            }
            
            .destination-card {
                flex: 0 0 auto;
                width: 280px;
                border-radius: 12px;
                overflow: hidden;
                box-shadow: 0 5px 15px rgba(0,0,0,0.1);
                transition: all 0.3s ease;
                background: white;
            }
            
            .location-badge {
                position: absolute;
                bottom: 15px;
                left: 15px;
                background: rgba(0,0,0,0.7);
                color: white;
                padding: 5px 10px;
                border-radius: 20px;
                font-size: 0.8rem;
                display: flex;
                align-items: center;
            }
            
            .destinations-arrows .arrow-btn[style*="hidden"] {
                opacity: 0.3;
                cursor: not-allowed;
            }
        </style>
    {% endblock %}

    {% block body %}
        <!-- Hero Section -->
        <header class="hero-section">
            <nav class="navbar navbar-expand-lg navbar-dark fixed-top">
        <div class="container">
            <a class="navbar-brand" href="#">
                <img src="{{ asset('images/logo.png') }}" alt="Logo" width="50" height="50">
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item"><a class="nav-link" href="#">Home</a></li>
                    
                    <li class="nav-item"><a class="nav-link" href="#">Hébergement</a></li>
                    <li class="nav-item"><a class="nav-link" href="#">Espace de coworking</a></li>
                    <li class="nav-item"><a class="nav-link" href="#">Événements</a></li>
                    <li class="nav-item"><a class="nav-link" href="#">Avis et réclamation</a></li>
                </ul>

                {% if user %}
        <span class="navbar-text text-white me-3">
            Bonjour, {{ user.nom }} {{ user.prenom }}
        </span>
        <a href="{{ path('app_mon_compte') }}" class="btn btn-outline-light me-2">Mon compte</a>
        <a href="{{ path('app_logout') }}" class="btn btn-danger">Déconnexion</a>
    {% else %}
        <a href="{{ path('app_login') }}" class="btn btn-outline-light me-2">Connexion</a>
        <a href="{{ path('app_register') }}" class="btn btn-danger">Inscription</a>
    {% endif %}
            </div>
        </div>
    </nav>

            <div class="hero-text container">
                <h1 class="animate__animated animate__fadeInDown">Commencez votre inoubliable voyage d'affaires avec nous.</h1>
                <p class="animate__animated animate__fadeInUp animate__delay-1s">Le meilleur voyage pour votre voyage commence dès maintenant.</p>
            </div>
            
        <div class="booking-form animate__animated animate__fadeInUp animate__delay-2s">
        <form class="row g-3 align-items-end">
            <div class="col-md-4">
                <div class="form-group">
                    <label class="form-label fw-bold">Destination</label>
                    <select class="form-select form-select-lg">
                        <option selected disabled>Choisissez une destination</option>
                        <option>Dubai</option>
                        <option>Paris</option>
                        <option>New York</option>
                        <option>Lisbonne</option>
                        <option>Rome</option>
                    </select>
                </div>
            </div>
            
            <div class="col-md-2">
                <div class="form-group">
                    <label class="form-label fw-bold">Personnes</label>
                    <input type="number" class="form-control form-control-lg" value="2" min="1">
                </div>
            </div>
            
            <div class="col-md-2">
                <div class="form-group">
                    <label class="form-label fw-bold">Arrivée</label>
                    <input type="date" class="form-control form-control-lg">
                </div>
            </div>
            
            <div class="col-md-2">
                <div class="form-group">
                    <label class="form-label fw-bold">Départ</label>
                    <input type="date" class="form-control form-control-lg">
                </div>
            </div>
            
            <div class="col-md-2">
                <button class="btn btn-danger btn-lg w-100 py-2">
                    <i class="fas fa-search me-2"></i> Rechercher
                </button>
            </div>
        </form>
    </div>
        </header>
>>>>>>> c6a1bbbd7cabd280f7efea996bf5898048c9380f

    <!-- Offers Section -->
    <section class="offers-section">
        <div class="container">
            <div class="page-header animate__animated animate__fadeIn">
                <h1 class="page-title">{{ message }}</h1>
                <div class="nav-arrows">
                    <button class="arrow-btn"><i class="fas fa-chevron-left"></i></button>
                    <button class="arrow-btn"><i class="fas fa-chevron-right"></i></button>
                </div>
            </div>
            
            <div class="row">
                {% for offre in offres %}
                <div class="col-lg-4 col-md-6 mb-4">
                    <div class="card-special">
                        <div class="card-img-container">
                            <img src="{{ asset('images/' ~ offre.image) }}" class="card-img-top" alt="{{ offre.ville }}">
                            <span class="card-tag">{{ offre.titre_droite }}</span>
                        </div>
                        
                        <div class="card-body">
                            <h4 class="card-title">{{ offre.ville }}</h4>
                            <div class="star-rating">
                                {% for i in 1..5 %}
                                    {% if i <= offre.notation %}<i class="fas fa-star"></i>
                                    {% else %}<i class="far fa-star"></i>
                                    {% endif %}
                                {% endfor %}
                            </div>
                            <p class="card-text">{{ offre.description }}</p>
                            <div class="price-container">
                                <span class="price-tag">From €{{ offre.prix }}</span>
                                <button class="btn btn-details">DETAILS <i class="fas fa-arrow-right ml-2"></i></button>
                            </div>
                        </div>
                    </div>
                </div>
                {% endfor %}
            </div>
        </div>
    </section>
    <!-- Events Section -->
<section class="events-section py-5 bg-light">
    <div class="container">
        <div class="text-center mb-5">
            <h2 class="section-title">Nos évènements</h2>
            <p class="citation-text fs-4 text-muted">{{ citation }}</p>
        </div>

        <div class="row g-4">
            {% for event in evenements %}
            <div class="col-lg-6">
                <div class="event-card card border-0 shadow-sm h-100 overflow-hidden">
                    <div class="row g-0 h-100">
                        <div class="col-md-5">
                            <div class="event-image-container h-100">
                                <img src="{{ asset('images/' ~ event.image) }}" class="img-fluid h-100 w-100 object-cover" alt="{{ event.titre }}">
                                <span class="event-tag">{{ event.tag }}</span>
                            </div>
                        </div>
                        <div class="col-md-7">
                            <div class="card-body p-4 d-flex flex-column h-100">
                                <h3 class="event-title">{{ event.titre }}</h3>
                                <div class="star-rating mb-2">
                                    {% for i in 1..5 %}
                                        {% if i <= event.notation %}<i class="fas fa-star text-warning"></i>
                                        {% else %}<i class="far fa-star text-warning"></i>
                                        {% endif %}
                                    {% endfor %}
                                </div>
                                <p class="event-description flex-grow-1">{{ event.description }}</p>
                                <div class="d-flex justify-content-between align-items-end">
                                    <span class="event-duration">{{ event.duree }}</span>
                                    <span class="event-price">{{ event.prix }}</span>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            {% endfor %}
        </div>

        <div class="text-center mt-5">
            <div class="inspiration-box p-4 d-inline-block bg-white shadow-sm">
                <p class="mb-0 fst-italic">"Une aventure te plaire."</p>
            </div>
        </div>
    </div>
</section>
<!-- Popular Destinations Section -->
<section class="destinations-section py-5 bg-white">
        <div class="container">
            <div class="section-header d-flex justify-content-between align-items-end mb-5">
                <div>
                    <h2 class="section-title">EX Destinations populaires</h2>
                    <p class="section-subtitle text-muted">{{ pop_dest_text }}</p>
                </div>
                <div class="destinations-arrows">
                    <button class="arrow-btn prev-dest" style="visibility: hidden;">
                        <i class="fas fa-chevron-left"></i>
                    </button>
                    <button class="arrow-btn next-dest">
                        <i class="fas fa-chevron-right"></i>
                    </button>
                </div>
            </div>

            <div class="destinations-carousel">
                <div class="destinations-track">
                    {% for destination in destinations %}
                    <div class="destination-card">
                        <div class="card-img-top position-relative">
                            <img src="{{ asset('images/' ~ destination.image) }}" class="img-fluid w-100" alt="{{ destination.nom }}" style="height: 180px; object-fit: cover;">
                            <div class="location-badge">
                                <i class="fas fa-map-marker-alt me-1"></i>
                                <span>{{ destination.ville }}</span>
                            </div>
                            <div class="position-absolute top-0 end-0 bg-white px-3 py-1 m-3 rounded-pill">
                                <i class="fas fa-star text-warning"></i>
                                <span class="fw-bold">{{ destination.notation }}</span>
                            </div>
                        </div>
                        <div class="card-body">
                            <h5 class="card-title fw-bold">{{ destination.nom }}</h5>
                        </div>
                    </div>
                    {% endfor %}
                </div>
            </div>
        </div>
    </section>
 {% if user %}
<!-- User Profile Management Section -->
<<<<<<< HEAD
<section class="user-profile-section  py-5 bg-light">
=======
<section class="user-profile-section py-5 bg-light">
>>>>>>> c6a1bbbd7cabd280f7efea996bf5898048c9380f
    <div class="container">
        <h2 class="mb-4">Gérer mon compte</h2>

        <form method="POST" action="{{ path('app_update_user') }}">
    <div class="row g-3">
        <div class="col-md-6">
            <label for="nom" class="form-label">Nom</label>
            <input type="text" class="form-control" id="nom" name="nom" value="{{ user.nom }}" required>
            {% if form_errors.nom is defined %}
                {% for error in form_errors.nom %}
                    <div class="text-danger small">{{ error }}</div>
                {% endfor %}
            {% endif %}
        </div>

        <div class="col-md-6">
            <label for="prenom" class="form-label">Prénom</label>
            <input type="text" class="form-control" id="prenom" name="prenom" value="{{ user.prenom }}" required>
            {% if form_errors.prenom is defined %}
                {% for error in form_errors.prenom %}
                    <div class="text-danger small">{{ error }}</div>
                {% endfor %}
            {% endif %}
        </div>

        <div class="col-md-6">
            <label for="age" class="form-label">Âge</label>
            <input type="number" class="form-control" id="age" name="age" value="{{ user.age }}" required>
            {% if form_errors.age is defined %}
                {% for error in form_errors.age %}
                    <div class="text-danger small">{{ error }}</div>
                {% endfor %}
            {% endif %}
        </div>

        <div class="col-md-6">
            <label for="email" class="form-label">Email</label>
            <input type="email" class="form-control" id="email" name="email" value="{{ user.email }}" required>
            {% if form_errors.email is defined %}
                {% for error in form_errors.email %}
                    <div class="text-danger small">{{ error }}</div>
                {% endfor %}
            {% endif %}
        </div>

        <div class="col-md-6">
            <label for="password" class="form-label">Nouveau mot de passe</label>
            <input type="password" class="form-control" id="password" name="password" placeholder="Laisser vide si inchangé">
            {% if form_errors.password is defined %}
                {% for error in form_errors.password %}
                    <div class="text-danger small">{{ error }}</div>
                {% endfor %}
            {% endif %}
        </div>
    </div>

    <div class="mt-4 d-flex justify-content-between">
        <button type="submit" class="btn btn-primary">Mettre à jour mes infos</button>

        <form method="POST" action="{{ path('app_delete_user') }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer votre compte ? Cette action est irréversible.');">
            <button type="submit" class="btn btn-outline-danger">Supprimer mon compte</button>
        </form>
    </div>
</form>

    </div>
</section>
{% endif %}


{% endblock %}

{% block javascripts %}
    {{ parent() }}
   <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        // Carrousel des destinations
        document.addEventListener('DOMContentLoaded', function() {
            const track = document.querySelector('.destinations-track');
            const prevBtn = document.querySelector('.prev-dest');
            const nextBtn = document.querySelector('.next-dest');
            const cards = document.querySelectorAll('.destination-card');
            
            if (cards.length > 0) {
                const cardStyle = window.getComputedStyle(cards[0]);
                const cardMarginRight = parseFloat(cardStyle.marginRight);
                const cardWidth = cards[0].offsetWidth + cardMarginRight;
                const visibleCards = Math.floor(document.querySelector('.destinations-carousel').offsetWidth / cardWidth);
                let currentPosition = 0;
                const maxPosition = -(cards.length - visibleCards) * cardWidth;

                nextBtn.addEventListener('click', () => {
                    currentPosition = Math.max(currentPosition - (visibleCards * cardWidth), maxPosition);
                    track.style.transform = `translateX(${currentPosition}px)`;
                    updateArrows();
                });

                prevBtn.addEventListener('click', () => {
                    currentPosition = Math.min(currentPosition + (visibleCards * cardWidth), 0);
                    track.style.transform = `translateX(${currentPosition}px)`;
                    updateArrows();
                });

                function updateArrows() {
                    prevBtn.style.visibility = currentPosition === 0 ? 'hidden' : 'visible';
                    nextBtn.style.visibility = currentPosition <= maxPosition ? 'hidden' : 'visible';
                }

                // Initial setup
                updateArrows();
                
                // Handle window resize
                window.addEventListener('resize', function() {
                    const newVisibleCards = Math.floor(document.querySelector('.destinations-carousel').offsetWidth / cardWidth);
                    const newMaxPosition = -(cards.length - newVisibleCards) * cardWidth;
                    
                    if (currentPosition < newMaxPosition) {
                        currentPosition = newMaxPosition;
                        track.style.transform = `translateX(${currentPosition}px)`;
                    }
                    
                    updateArrows();
                });
            }

            // Navbar scroll effect
            window.addEventListener('scroll', function() {
                document.querySelector('.navbar').classList.toggle('scrolled', window.scrollY > 100);
            });
        });
    </script>
{% endblock %}