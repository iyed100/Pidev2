{% extends 'admin/base_admin.html.twig' %}

{% block body %}
<!-- Stats Row -->
<div class="row mb-4">
    <!-- Reservations Card -->
    <div class="col-xl-3 col-md-6 mb-4">
        <div class="card-elegant card border-left-primary h-100 py-2">
            <div class="card-body">
                <div class="d-flex justify-content-between">
                    <div>
                        <div class="text-xs font-weight-bold text-primary mb-1">Réservations</div>
                        <div class="h5 mb-0 font-weight-bold text-gray-800">{{ reclamations_count }}</div>
                        <small class="text-success">Total des réservations</small>
                    </div>
                    <i class="fas fa-exclamation-triangle fa-2x text-gray-300"></i>
                </div>
            </div>
        </div>
    </div>

    <!-- Assurances Card -->
    <div class="col-xl-3 col-md-6 mb-4">
        <div class="card-elegant card border-left-success h-100 py-2">
            <div class="card-body">
                <div class="d-flex justify-content-between">
                    <div>
                        <div class="text-xs font-weight-bold text-success mb-1">Assurances</div>
                        <div class="h5 mb-0 font-weight-bold text-gray-800">{{ assurances_count }}</div>
                        <small class="text-success">Total des assurances</small>
                    </div>
                    <i class="fas fa-shield-alt fa-2x text-gray-300"></i>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Assurances par type -->
<div class="row mb-4">
    <div class="col-lg-12">
        <div class="card shadow h-100">
            <div class="card-header py-3">
                <h6 class="m-0 font-weight-bold text-primary">Assurances par type</h6>
            </div>
            <div class="card-body">
                {% if chart_labels|length > 0 and chart_labels|length == chart_data|length %}
                    <ul class="list-group list-group-flush">
                        {% for index in 0..(chart_labels|length - 1) %}
                            <li class="list-group-item d-flex justify-content-between align-items-center">
                                <span>{{ chart_labels[index]|default('Inconnu') }}</span>
                                <span class="badge bg-primary text-white">{{ chart_data[index]|default(0) }}</span>
                            </li>
                        {% endfor %}
                    </ul>
                {% else %}
                    <p class="text-muted">Aucune donnée disponible pour les assurances par type.</p>
                {% endif %}
            </div>
        </div>
    </div>
</div>

<!-- Réservations par hôtel -->
<div class="row mb-4">
    <div class="col-lg-12">
        <div class="card shadow h-100">
            <div class="card-header py-3">
                <h6 class="m-0 font-weight-bold text-primary">Réservations par hôtel</h6>
            </div>
            <div class="card-body">
                {% if hotel_labels|length > 0 and hotel_labels|length == hotel_data|length %}
                    <ul class="list-group list-group-flush">
                        {% for index in 0..(hotel_labels|length - 1) %}
                            <li class="list-group-item d-flex justify-content-between align-items-center">
                                <span>{{ hotel_labels[index]|default('Inconnu') }}</span>
                                <span class="badge bg-primary text-white">{{ hotel_data[index]|default(0) }}</span>
                            </li>
                        {% endfor %}
                    </ul>
                {% else %}
                    <p class="text-muted">Aucune donnée disponible pour les réservations par hôtel.</p>
                {% endif %}
            </div>
        </div>
    </div>
</div>

<!-- Assurances par statut et Réservations par statut (côte à côte) -->
<div class="row mb-4">
    <!-- Assurances par statut -->
    <div class="col-lg-6 mb-4">
        <div class="card shadow h-100">
            <div class="card-header py-3">
                <h6 class="m-0 font-weight-bold text-primary">Assurances par statut</h6>
            </div>
            <div class="card-body">
                {% if status_labels|length > 0 and status_labels|length == status_data|length %}
                    <ul class="list-group list-group-flush">
                        {% for index in 0..(status_labels|length - 1) %}
                            <li class="list-group-item d-flex justify-content-between align-items-center">
                                <span>{{ status_labels[index]|default('Inconnu') }}</span>
                                <span class="badge bg-primary text-white">{{ status_data[index]|default(0) }}</span>
                            </li>
                        {% endfor %}
                    </ul>
                {% else %}
                    <p class="text-muted">Aucune donnée disponible pour les assurances par statut.</p>
                {% endif %}
            </div>
        </div>
    </div>

    <!-- Réservations par statut -->
    <div class="col-lg-6 mb-4">
        <div class="card shadow h-100">
            <div class="card-header py-3">
                <h6 class="m-0 font-weight-bold text-primary">Réservations par statut</h6>
            </div>
            <div class="card-body">
                {% if reservation_status_labels|length > 0 and reservation_status_labels|length == reservation_status_data|length %}
                    <ul class="list-group list-group-flush">
                        {% for index in 0..(reservation_status_labels|length - 1) %}
                            <li class="list-group-item d-flex justify-content-between align-items-center">
                                <span>{{ reservation_status_labels[index]|default('Inconnu') }}</span>
                                <span class="badge bg-primary text-white">{{ reservation_status_data[index]|default(0) }}</span>
                            </li>
                        {% endfor %}
                    </ul>
                {% else %}
                    <p class="text-muted">Aucune donnée disponible pour les réservations par statut.</p>
                {% endif %}
            </div>
        </div>
    </div>
</div>

<!-- Assurances par client -->
<div class="row mb-4">
    <div class="col-lg-12">
        <div class="card shadow h-100">
            <div class="card-header py-3">
                <h6 class="m-0 font-weight-bold text-primary">Assurances par client</h6>
            </div>
            <div class="card-body">
                {% if client_labels|length > 0 and client_labels|length == client_data|length %}
                    <ul class="list-group list-group-flush">
                        {% for index in 0..(client_labels|length - 1) %}
                            <li class="list-group-item d-flex justify-content-between align-items-center">
                                <span>{{ client_labels[index]|default('Inconnu') }}</span>
                                <span class="badge bg-primary text-white">{{ client_data[index]|default(0) }}</span>
                            </li>
                        {% endfor %}
                    </ul>
                {% else %}
                    <p class="text-muted">Aucune donnée disponible pour les assurances par client.</p>
                {% endif %}
            </div>
        </div>
    </div>
</div>

<!-- Réservations par client -->
<div class="row mb-4">
    <div class="col-lg-12">
        <div class="card shadow h-100">
            <div class="card-header py-3">
                <h6 class="m-0 font-weight-bold text-primary">Réservations par client</h6>
            </div>
            <div class="card-body">
                {% if reservation_client_labels|length > 0 and reservation_client_labels|length == reservation_client_data|length %}
                    <ul class="list-group list-group-flush">
                        {% for index in 0..(reservation_client_labels|length - 1) %}
                            <li class="list-group-item d-flex justify-content-between align-items-center">
                                <span>{{ reservation_client_labels[index]|default('Inconnu') }}</span>
                                <span class="badge bg-primary text-white">{{ reservation_client_data[index]|default(0) }}</span>
                            </li>
                        {% endfor %}
                    </ul>
                {% else %}
                    <p class="text-muted">Aucune donnée disponible pour les réservations par client.</p>
                {% endif %}
            </div>
        </div>
    </div>
</div>

<!-- Réservations par espace de coworking et par transport (côte à côte) -->
<div class="row mb-4">
    <!-- Réservations par espace de coworking -->
    <div class="col-lg-6 mb-4">
        <div class="card shadow h-100">
            <div class="card-header py-3">
                <h6 class="m-0 font-weight-bold text-primary">Réservations par espace de coworking</h6>
            </div>
            <div class="card-body">
                {% if coworking_labels|length > 0 and coworking_labels|length == coworking_data|length %}
                    <ul class="list-group list-group-flush">
                        {% for index in 0..(coworking_labels|length - 1) %}
                            <li class="list-group-item d-flex justify-content-between align-items-center">
                                <span>{{ coworking_labels[index]|default('Inconnu') }}</span>
                                <span class="badge bg-primary text-white">{{ coworking_data[index]|default(0) }}</span>
                            </li>
                        {% endfor %}
                    </ul>
                {% else %}
                    <p class="text-muted">Aucune donnée disponible pour les réservations par espace de coworking.</p>
                {% endif %}
            </div>
        </div>
    </div>

    <!-- Réservations par transport -->
    <div class="col-lg-6 mb-4">
        <div class="card shadow h-100">
            <div class="card-header py-3">
                <h6 class="m-0 font-weight-bold text-primary">Réservations par transport</h6>
            </div>
            <div class="card-body">
                {% if transport_labels|length > 0 and transport_labels|length == transport_data|length %}
                    <ul class="list-group list-group-flush">
                        {% for index in 0..(transport_labels|length - 1) %}
                            <li class="list-group-item d-flex justify-content-between align-items-center">
                                <span>{{ transport_labels[index]|default('Inconnu') }}</span>
                                <span class="badge bg-primary text-white">{{ transport_data[index]|default(0) }}</span>
                            </li>
                        {% endfor %}
                    </ul>
                {% else %}
                    <p class="text-muted">Aucune donnée disponible pour les réservations par transport.</p>
                {% endif %}
            </div>
        </div>
    </div>
</div>


{% endblock %}

{% block javascripts %}
{{ parent() }}
{# Aucun JavaScript requis #}
{% endblock %}