<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Elsagent Dashboard{% endblock %}</title>
    <!-- Bootstrap 5 -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --sidebar-width: 280px;
            --sidebar-bg: linear-gradient(195deg, #4e73df 0%, #224abe 100%);
            --card-radius: 0.5rem;
        }
        
        /* Sidebar stylisée */
        .sidebar {
            width: var(--sidebar-width);
            height: calc(100vh - 40px);
            position: fixed;
            left: 20px;
            top: 20px;
            background: var(--sidebar-bg);
            border-radius: 0 20px 20px 0;
            box-shadow: 10px 0 30px rgba(0, 0, 0, 0.1);
            z-index: 1000;
            overflow: hidden;
        }
        
        .sidebar-brand {
            height: 120px;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            padding: 20px;
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        }
        
        .sidebar-brand img {
            width: 60px;
            height: 60px;
            margin-bottom: 10px;
            border-radius: 50%;
            object-fit: cover;
            border: 2px solid white;
            transition: transform 0.5s ease;
        }
        
        .sidebar-brand:hover img {
            transform: rotate(15deg);
        }
        
        .sidebar-brand span {
            color: white;
            font-weight: 800;
            font-size: 1.2rem;
            letter-spacing: 1px;
        }
        
        .sidebar-item {
            padding: 12px 25px;
            color: rgba(255, 255, 255, 0.8);
            border-left: 3px solid transparent;
            transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
        }
        
        .sidebar-item:hover, .sidebar-item.active {
            background: rgba(255, 255, 255, 0.2);
            color: white;
            border-left: 3px solid white;
            transform: scale(1.05);
        }
        
        /* Contenu principal */
        .main-content {
            margin-left: 320px;
            padding: 30px;
            background: #f8f9fc;
            min-height: 100vh;
        }
        
        /* Navbar supérieure */
        .top-navbar {
            background: white;
            border-radius: var(--card-radius);
            box-shadow: 0 0.15rem 1.75rem 0 rgba(58, 59, 69, 0.15);
            padding: 15px 25px;
        }
        
        /* Cartes modernes */
        .card-elegent {
            border: none;
            border-radius: var(--card-radius);
            box-shadow: 0 0.15rem 1.75rem 0 rgba(58, 59, 69, 0.1);
            transition: transform 0.3s;
        }
        
        .card-elegent:hover {
            transform: translateY(-5px);
        }
        
        /* Icône notification */
        .notification-badge {
            position: absolute;
            top: -5px;
            right: -5px;
            width: 20px;
            height: 20px;
            background: #e74a3b;
            color: white;
            border-radius: 50%;
            font-size: 0.7rem;
            display: flex;
            align-items: center;
            justify-content: center;
        }
    </style>
</head>
<body>
    <!-- Sidebar -->
    <!-- Sidebar -->
    <div class="sidebar">
    <div class="sidebar-brand">
        <img src="https://via.placeholder.com/60x60" alt="Logo">
        <span>Elsagent</span>
    </div>
    
    <div class="sidebar-menu pt-3">
        {# TEMPORARY: Remove dashboard link until route exists - coworkers should reimplement #}
        {# <div class="sidebar-item">
            <a href="{{ path('dashboard') }}" class="sidebar-item">
                <i class="fas fa-fw fa-tachometer-alt me-3"></i> Dashboard
            </a>
        </div> #}

        {# TEMPORARY: Remove userList link until route exists - coworkers should reimplement #}
        {# <div class="sidebar-item">
            <a href="{{ path('userList') }}" class="sidebar-item">
                <i class="fas fa-fw fa-users me-3"></i> Users list
            </a>
        </div> #}

        {# RAYEN'S AVIS ADMIN SECTION - IMPLEMENTED #}
        <div class="sidebar-item {{ app.request.attributes.get('_route') starts with 'admin_avis' ? 'active' : '' }}">
            <a href="{{ path('admin_avis') }}" class="sidebar-item">
                <i class="fas fa-fw fa-star me-3"></i> Avis Management
            </a>
        </div>

        {# COWORKERS: Add your custom admin links here with the same structure #}
        <!--
        
        -->
    </div>
</div>
   

    <!-- Main Content -->
    <div class="main-content">
        <!-- Top Navbar -->
        <div class="top-navbar mb-4 d-flex justify-content-between align-items-center">
            <h4 class="mb-0">Dashboard</h4>
            <div class="d-flex align-items-center">
                <div class="position-relative me-3">
                    <i class="fas fa-bell fa-lg text-gray-500"></i>
                    <span class="notification-badge">3</span>
                </div>
               {#<a href="{{ path('app_logout') }}" class="btn btn-outline-danger btn-sm">#}
                    <i class="fas fa-sign-out-alt me-1"></i> Déconnexion
                </a>
            </div>
        </div>

        {% block body %}{% endblock %}
    </div>

    <!-- Scripts -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>